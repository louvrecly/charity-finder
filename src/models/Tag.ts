export type Tag = string;

export interface TagDetails {
  id: string;
  tagName: string;
  title: string;
  causeCategory: string;
  tagImageCloudinaryId: string;
  tagImageUrl: string;
  tagUrl: string;
}
